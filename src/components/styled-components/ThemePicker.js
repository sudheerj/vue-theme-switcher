import styled from 'vue-styled-components';

const ThemePicker = styled.div`
    margin-left: 85%;
    color: white;
    font-size: 15px;
    & > span {
        margin-top:-10px;
    }
`;

export default ThemePicker;
